import pricesService from "./PricesService";

describe('Prices Service', () => {

    beforeAll(() => {
        // jest
    })
    describe('testAllModels', () => {

        // it('tests all models', async () => {
        //     const data = await pricesService.testAllModels();

        //     console.log('>>>>>>>>>>>>>>>>>>>>>>>>>>');
        //     console.log(data);
    
        //     Object.values(data).forEach(v => {
        //         expect(v > 0).toBeTruthy();
        //     })
        // }, 10000);

        it('foo', () => {
            expect(true).toBeTruthy();
        });

    });
});